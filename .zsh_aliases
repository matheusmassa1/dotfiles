#!/usr/bin/env bash

# Dotfiles repo
alias dtf='git --git-dir=$HOME/.dtf/.git/ --work-tree=$HOME'

# Emacs
alias emacs-start='emacs --daemon && emacsclient -c -n'

# Utils
alias k="kill -9"
alias ll="ls -l"

source $HOME/.nubank_aliases 2> /dev/null
